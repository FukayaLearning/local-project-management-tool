---
name: build
description: プロジェクトのビルドを実行します。
---

# ビルド (build)

このスキルは、プロジェクトのビルド（Dockerイメージの作成）を行うために使用されます。プロジェクトルートにある `build.sh` を呼び出します。

## 実行方法

```bash
bash build.sh
```

## 注意事項

- 実行には Docker が必要です。
- スクリプトはプロジェクトルートから実行することを想定しています。
