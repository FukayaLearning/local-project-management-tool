---
description: 現在のドキュメント変更点から英語版と日本語版を同期させる
---

## やること

- 現在のプロジェクトにおける英語版（`.md` / `.yaml`）あるいは日本語版（`.ja.md` / `.ja.yaml`）のドキュメントの `git diff` を取得し、変更点を把握する。
- 取得したドキュメント変更点から、プロジェクトルール `documents.md` の「ドキュメント作成共通ルール（両言語で表現内容を同じにする）」に従い、もう一方の言語版のドキュメントが内容的に同期するように記載を追加・修正する。
